# M1.1 — Certification Harness Discovery Spike

Mode: Discovery. Sprint: M1.1. Status: Traceability only.

No M1 product feature was implemented. No WP1–WP10 work was performed. No production code, UI, migration, schema, provider behaviour, provider classification, CI workflow, or certification semantic was modified. No certification row, profile, or registry evidence entry was created. M1 is not authorized by this document.

This spike creates exactly one file: this document.

---

## 1. Scope and authoritative inputs

### 1.1 Inputs read

| Requested path                              | Actual path in repository                        | Note                                                      |
| ------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------- |
| `docs/m1/M1-Implementation-Plan.md`         | Same                                              | Created in sprint M1.0 immediately preceding this spike     |
| `docs/m1/M1-Backlog.md`                     | Same                                              | M1.0                                                        |
| `docs/m1/M1-Dependency-Graph.md`            | Same                                              | M1.0                                                        |
| `docs/m1/M1-Certification-Checklist.md`     | Same                                              | M1.0                                                        |
| `docs/m1/M1-Risk-Register.md`               | Same                                              | M1.0                                                        |
| `docs/blueprint/K-launch-certification.md`  | Same                                              | Authoritative row definitions, lines 73–106                 |
| `docs/blueprint/B-capability-matrix.md`     | Same                                              | Capability IDs, lines 45–49                                 |
| `docs/blueprint/D-milestone-roadmap.md`     | Same                                              | M1 envelope and row set, §M1                                |
| `docs/blueprint/I-governance.md`            | Same                                              | ADR lifecycle, M0.5 infrastructure limit                    |
| `docs/registry/required-evidence.json`      | Same                                              | v1.0.0, 24 mandatory entries                                |
| `tests/certification/`                      | Same                                              | 7 spec files across 6 directories                           |
| `tests/`                                    | Same                                              | Contains only `tests/certification/`; no unit-test tree     |
| `src/`                                      | Same                                              | Clean-architecture layers present                           |
| `package.json`                              | Same                                              | Certification scripts enumerated in §5                      |
| `.github/workflows/`                        | Same                                              | 7 workflows                                                 |

### 1.2 Path discrepancies recorded

- **X-01.** There is no unit or integration test tree. `tests/` contains only `tests/certification/`. Any M1 row expecting non-Playwright evidence has no home today.
- **X-02.** `docs/certification/Certification-Profiles.md` is the profile document; the request did not name it, but it is authoritative for PROF-01..09 and is used here.
- **X-03.** `playwright.config.ts` defines projects `web-chromium`, `web-firefox`, `web-webkit`. There is no `web-mobile` project.

### 1.3 Pre-flight verification carried forward from M1.0

Confirmed present and internally consistent: the frozen blueprint, `required-evidence.json` v1.0.0, the M0.6 gate libraries (`scripts/lib/result-state.mjs`, `scripts/lib/evidence-io.mjs`, `scripts/check-gates.mjs`), `gates:check` inside `npm run verify`, sealed run `RUN-M0R-001` with 25 records, and zero Tier A / Tier B claims. Discrepancies D-01 through D-05 from `M1-Implementation-Plan.md` §1 apply here unchanged; D-01 (no M1 row in the registry) and D-05 (no web-mobile project) are the two that directly determine row status below.

---

## 2. Traceability matrix

One row per certification ID. The 21 required columns are split across two tables sharing the Certification ID key, because a single 21-column table is unreadable. Table A carries columns 1–10, Table B carries columns 11–21. Where no path exists the cell reads **None found** — no path is guessed.

### Table A — definition and existing assets

| Certification ID | Exact definition (from K-launch-certification.md) | Capability ID | Tier | Required profile | User journey | Existing module/path | Existing test/path | Existing fixture/path | Existing evidence type |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CERT-ROOM-01 | Invite link lands the user in the intended room, including across sign-in | n/a | C (envelope default) | PROF-01, PROF-05 | Receive invite → open link → sign in if needed → land in room | `src/domain/rooms/room-flow-service.ts`, `src/routes/join.$code.tsx`, `src/features/invitations/` | None found | `tests/certification/fixtures/backend.ts` (auth identities only) | Automated journey record, invite → room surface |
| CERT-ROOM-02 | Member appears to all peers with correct identity and role | n/a | C | PROF-01, PROF-07 | Join room → all peers observe member | `src/domain/rooms/room-flow-service.ts`, `room-read-model.ts`, `src/features/waiting-room/use-room-realtime.ts` | None found | `tests/certification/fixtures/backend.ts` | Multi-client journey record |
| CERT-ROOM-03 | 9th joiner is refused with a clear message | n/a | C | PROF-07 | 8 members present → 9th attempts join → refused | `src/domain/rooms/room-flow-service.ts`, `src/shared/constants/system-constants.ts` | None found | `tests/certification/fixtures/backend.ts` | Deterministic automated record |
| CERT-ROOM-04 | Rejoin within grace restores room context | n/a | C | PROF-04 | Member leaves → rejoins within grace → context restored | `src/domain/rooms/room-flow-service.ts`, `presence-coordinator.ts` | None found | `tests/certification/fixtures/backend.ts` | Automated record, Leave/Rejoin profile |
| CERT-PRES-01 | Readiness state is identical for all participants | n/a | C | PROF-07 | All members mark ready → state converges | `src/domain/rooms/ready-coordinator.ts`, `src/features/waiting-room/use-room-ready.ts` | None found | `tests/certification/fixtures/backend.ts` | Multi-client assertion record |
| CERT-PRES-02 | Dropped member is marked absent within threshold (≤ 10 s) | n/a | C | PROF-04 | Member transport severed → peers mark absent | `src/domain/rooms/presence-coordinator.ts`, `src/features/waiting-room/use-room-presence.ts` | None found | `tests/certification/fixtures/backend.ts` | Measured latency record |
| CERT-WP-01 | All participants reach zero within spread | n/a | C | PROF-01, PROF-02, PROF-07 | Host starts countdown → all clients reach zero | `src/domain/countdown/countdown-runtime.ts`, `countdown-machine.ts`, `src/domain/rooms/countdown-coordinator.ts` | None found | `tests/certification/fixtures/backend.ts` | Instrumented timestamp record |
| CERT-WP-02 | Stages advance identically for host and members | n/a | C | PROF-07 | Invite → Waiting → Ready → Countdown → Watching | `src/features/waiting-room/waiting-room-state.ts`, `src/features/watch-party/` | None found | `tests/certification/fixtures/backend.ts` | Multi-client journey record |
| CERT-SYNC-C-01 | Deep link opens, countdown coordinates, no false sync UI | CAP-OTT-WEBDESK | C | PROF-01 | Pick title → countdown → manual launch on each client | `src/domain/providers/deep-link-service.ts`, `provider-launch-coordinator.ts`, `manual-sync-guidance.ts` | None found | `tests/certification/fixtures/backend.ts` | Journey + UI assertion record |
| CERT-SYNC-C-02 | As CERT-SYNC-C-01, on web mobile | CAP-OTT-WEBMOB | C | PROF-01 | Same journey on a mobile viewport | Same modules as CERT-SYNC-C-01 | None found | None found (no mobile project) | Journey + UI assertion record |
| CERT-PROV-01 | Capability tier and consequence stated before commit | all B rows | C | PROF-01 | Browse services → tier and consequence shown before commit | `src/domain/providers/provider-tier.ts`, `capability-certification.ts`, `src/features/providers/` | None found (`tests/certification/provider/capability-tier.spec.ts` exists but emits `CERT-PROV-A1-*`, a different row) | `tests/certification/fixtures/backend.ts` | UI assertion per capability row |
| CERT-PROV-02 | One-step fallback, announced, reversible | all B rows | C | PROF-01, PROF-04 | Provider launch fails → fallback offered, announced, reversible | `src/domain/providers/provider-launcher.ts`, `provider-launch-coordinator.ts`, `provider-session.ts` | None found | `tests/certification/fixtures/backend.ts` | Fault-injection record |
| CERT-EXP-01 | WCAG 2.1 AA on all launch surfaces | n/a | C | PROF-09 | Keyboard and screen-reader traversal of every launch surface | `src/foundation/accessibility/accessibility-provider.tsx`, all route components | `tests/certification/accessibility/a11y-sweep.spec.ts` (emits `CERT-A11Y-<route>-<browser>`, not `CERT-EXP-01`) | None found | Automated axe record + manual SR pass |
| CERT-EXP-02 | Motion respects the OS preference everywhere | n/a | C | PROF-09 | Enable reduced motion → all animation respects it | `src/foundation/accessibility/accessibility-provider.tsx`, `src/styles.css`, `src/features/waiting-room/components/countdown-panel.tsx` | None found | None found | Automated record, 100% of motion surfaces |

### Table B — status, dependency and next action

| Certification ID | Existing evidence writer | Registry presence | Workflow presence | Current status | Missing dependency | Allowed change category | Blocking severity | Owner/engine | Recommended next action | Evidence needed before execution | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CERT-ROOM-01 | Generic only: `tests/certification/helpers/evidence.ts` `recordEvidence()`. No call site for this ID | Absent from `docs/registry/required-evidence.json` | `.github/workflows/playwright.yml` runs the whole `testDir`, so a new spec executes automatically | Harness missing | Spec file emitting `CERT-ROOM-01`; multi-identity invite fixture | Test-only, then registry-only | Blocker | Room | Author `tests/certification/room/invite-resolution.spec.ts` under WP1/WP3 | Two identities, one room, one live invite code | Product path exists and is exercised manually; only certification coverage is absent |
| CERT-ROOM-02 | Generic only | Absent | Generic (playwright.yml) | Harness missing | Spec emitting `CERT-ROOM-02`; N-client fixture | Test-only, then registry-only | Blocker | Room | Author a multi-client join spec under WP1/WP4 | ≥ 2 authenticated identities under PROF-07 | Realtime hub exists at `src/features/waiting-room/room-realtime-hub.ts` |
| CERT-ROOM-03 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-ROOM-03`; 9 provisioned identities | Test-only, then registry-only | Major | Room | Author a capacity-refusal spec under WP4 | 9 identities; capacity constant read from source, not hard-coded | Envelope is 2–8, so the 9th joiner is the refusal case |
| CERT-ROOM-04 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-ROOM-04`; PROF-04 transport severance helper | Test-only, then registry-only | Blocker | Room | Author a leave/rejoin spec under WP4 | Grace-window value sourced from production constants | PROF-04 is `supported`, so no environment blocker |
| CERT-PRES-01 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-PRES-01` | Test-only, then registry-only | Blocker | Presence | Author a readiness-convergence spec under WP5 | ≥ 3 identities under PROF-07 | — |
| CERT-PRES-02 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-PRES-02`; latency instrumentation | Test-only, then registry-only | Major | Presence | Author an absence-detection spec under WP5 | Measured detection latency, recorded as a measurement not an assertion | 10 s threshold must be measured; an unmeasured row is `unmeasured`, never `pass` |
| CERT-WP-01 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-WP-01`; per-client zero-timestamp instrumentation | Test-only, then registry-only | Blocker | Watch Party | Author a countdown-spread spec under WP6 | Per-client zero timestamps and the C4 spread budget | Budget lives in `docs/blueprint/C4-performance-budget.md`; do not restate it in test code |
| CERT-WP-02 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-WP-02` | Test-only, then registry-only | Major | Watch Party | Author a stage-progression spec under WP6 | Host + member client pair | Stage model already exists in `waiting-room-state.ts` |
| CERT-SYNC-C-01 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-SYNC-C-01`; deep-link interception without leaving the browser context | Test-only, then registry-only | Blocker | Sync | Author a Tier C coordination spec under WP7 | Assertion that no host-control affordance is presented | Must assert honesty of manual-sync UI, per ADR-014 |
| CERT-SYNC-C-02 | Generic only | Absent | Generic workflow exists, but no mobile project for it to run | **Environment unavailable** | No `web-mobile` project in `playwright.config.ts`; harness also missing | Certification-only (harness config) plus test-only | Blocker | Sync | WP8 discovery: decide between adding a mobile-viewport project or deferring the row by numbered ADR | Same as C-01 plus a mobile viewport surface | Adding a harness project may collide with the M0.5 infrastructure limit in `I-governance.md`; see §4 |
| CERT-PROV-01 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-PROV-01` covering every B-matrix launch row | Test-only, then registry-only | Blocker | Provider | Author a disclosure-sweep spec under WP9 | Enumeration of B-matrix launch rows and the surfaces that present them | `capability-tier.spec.ts` proves classification is tuple-based; it does not prove user-facing disclosure |
| CERT-PROV-02 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-PROV-02`; fault-injection helper for a failed launch | Test-only, then registry-only | Blocker | Provider | Author a degraded-mode spec under WP9 | An injectable launch failure and an observable, reversible fallback | Fault injection must not modify provider behaviour, only observe it |
| CERT-EXP-01 | Partial: `a11y-sweep.spec.ts` writes `CERT-A11Y-*` records via `recordEvidence()` | Absent under this ID; the registry requires `CERT-A11Y-` as a prefix entry with minimum 1 | Generic | Harness missing | A record under the `CERT-EXP-01` identity, plus route coverage extended to all launch surfaces | Test-only, then registry-only | Blocker | Experience | WP10: extend route coverage, then decide whether `CERT-EXP-01` rolls up the `CERT-A11Y-*` set or is emitted directly | Full launch-surface route list; zero AA violations across it | Closest row to runnable of the fourteen |
| CERT-EXP-02 | Generic only | Absent | Generic | Harness missing | Spec emitting `CERT-EXP-02` under an emulated reduced-motion preference | Test-only, then registry-only | Major | Experience | Author a reduced-motion spec under WP10 | Enumeration of motion surfaces; Playwright `reducedMotion` context option | Implementation exists in `accessibility-provider.tsx` and `src/styles.css`; only the assertion is missing |

---

## 3. Minimal change analysis

For each row that is not "Runnable now", the smallest possible next step, and whether it is permitted under the frozen Constitution and Launch Envelope.

| Certification ID | Smallest next step | Change category | Touches production code? | Permitted under frozen Constitution / Launch Envelope? |
| --- | --- | --- | --- | --- |
| CERT-ROOM-01 | Add one spec file + reuse existing evidence helper | Test-only | No | Yes — certification enablement, explicitly the purpose of the harness |
| CERT-ROOM-02 | Add one spec + N-identity fixture | Test-only | No | Yes |
| CERT-ROOM-03 | Add one spec provisioning 9 identities | Test-only | No | Yes |
| CERT-ROOM-04 | Add one spec using the PROF-04 helper | Test-only | No | Yes |
| CERT-PRES-01 | Add one spec | Test-only | No | Yes |
| CERT-PRES-02 | Add one spec plus latency instrumentation in the test layer | Test-only | No | Yes, provided instrumentation stays in tests; a production hook would be a production-code change requiring justification |
| CERT-WP-01 | Add one spec reading per-client zero timestamps | Test-only | No, if timestamps are observable from the DOM or event stream; otherwise production-code | Yes for the test-only path. If instrumentation must be added to `countdown-runtime.ts`, that is a production-code change and needs an M1 authorization, not this spike |
| CERT-WP-02 | Add one spec | Test-only | No | Yes |
| CERT-SYNC-C-01 | Add one spec with UI-honesty assertions | Test-only | No | Yes — and required by ADR-014 |
| CERT-SYNC-C-02 | Add a mobile-viewport project to `playwright.config.ts`, then a spec | Certification-only (harness config) + test-only | No | **Unresolved.** `docs/blueprint/I-governance.md` records a Constitutional Limit that M0.5 was the final infrastructure sprint. Whether a new harness project counts as infrastructure or as certification enablement is a governance question, not an engineering one. Requires a numbered ADR or an explicit human ruling before WP8 proceeds |
| CERT-PROV-01 | Add one disclosure-sweep spec | Test-only | No | Yes, provided `provider-tier.ts` and the capability matrix are read, never modified |
| CERT-PROV-02 | Add one fault-injection spec | Test-only | No | Yes, provided injection is observational and does not alter provider behaviour |
| CERT-EXP-01 | Extend the route list in the existing sweep and emit or roll up a `CERT-EXP-01` record | Test-only + registry-only | No | Yes. The registry edit is an addition to the mandatory set, which M0.6 semantics support without change |
| CERT-EXP-02 | Add one spec using Playwright's `reducedMotion` context option | Test-only | No | Yes |

**Aggregate:** thirteen of fourteen rows need **test-only** work plus a follow-on **registry-only** edit. One row (`CERT-SYNC-C-02`) needs a **certification-only** harness-configuration change whose permissibility is unresolved. **No row requires a schema change, a provider change, or an architecture change.** No row requires modifying certification semantics.

---

## 4. Voice dependency state (documented, not fixed)

| Item | Current repository state | Evidence |
| --- | --- | --- |
| CERT-VOICE-01 | `status: "blocked"`, detail: "Blocked by PROF-08 (unsupported): BLOCKING: no LiveKit test project is provisioned. Voice rows cannot be certified." | `tests/certification/evidence/RUN-M0R-001/records/CERT-VOICE-01.json` |
| CERT-VOICE-02 | Same status and cause | `tests/certification/evidence/RUN-M0R-001/records/CERT-VOICE-02.json` |
| PROF-08 | "Voice under load — two live audio publishers", marked **unsupported**: no media-server credentials in the certification environment | `docs/certification/Certification-Profiles.md` line 19; `tests/certification/profiles/certification-profiles.ts` line 107 |
| DEBT-005 | "PROF-08 voice profile unsupported — no LiveKit test project", severity High, blocking true, milestone **M3**, engine ENG-VOICE, ADR-007 | `docs/debt/debt-register.json` |
| Spec that exists | `tests/certification/voice/voice-transport.spec.ts` emits both voice IDs and correctly records `blocked` rather than `fail` | Spec lines 16, 28 |
| Registry | Both voice rows **are** in the mandatory set, unlike every M1 row | `docs/registry/required-evidence.json` |

**Relationship to M1.** `docs/blueprint/D-milestone-roadmap.md` §M1 lists `CERT-VOICE-01..02` inside the M1 row set, but the debt register schedules the enabling dependency at M3 and the profile registry marks it unsupported. The two rows are therefore **Blocked by dependency** for the whole of M1. Because `blocked` never counts as pass, an M1 seal containing these two rows is honest, but an M1 gate that requires them to pass can never be reached. Reconciling the roadmap with the debt register is a governance action, not an engineering one, and is out of scope here.

**Identifier conflict.** `docs/blueprint/J-technical-debt.md` line 15 uses `DEBT-005` for an unrelated ADR-header item at milestone M0. Two frozen documents assign the same identifier to different debts. Recorded, not fixed.

---

## 5. Harness and workflow inventory

| Asset | Path | Relevance to M1 rows |
| --- | --- | --- |
| Playwright config | `playwright.config.ts` | `testDir: ./tests/certification`, `testMatch: *.spec.ts`; projects `web-chromium`, `web-firefox`, `web-webkit`; workers 1; evidence under `tests/certification/evidence/<runId>/` |
| Evidence writer | `tests/certification/helpers/evidence.ts` | Single `recordEvidence()` contract; writes `records/<evidenceId>.json`. Reusable by every M1 row without modification |
| Run context | `tests/certification/helpers/run-context.ts` | Deterministic `RUN_ID`, `EVIDENCE_ROOT` |
| Profiles | `tests/certification/profiles/certification-profiles.ts` | PROF-01..09 with support flags; PROF-03 and PROF-08 `unsupported` |
| Backend fixture | `tests/certification/fixtures/backend.ts` | Publishable-key client only, `backendConfigured` guard; provides `CertIdentity`. Basis for the multi-identity fixture WP1 needs |
| Existing specs | `room/authorization.spec.ts`, `room/server-authority.spec.ts`, `realtime/baselines.spec.ts`, `provider/capability-tier.spec.ts`, `resilience/recovery.spec.ts`, `accessibility/a11y-sweep.spec.ts`, `voice/voice-transport.spec.ts` | Emit `CERT-AUTHZ-*`, `CERT-SA-*`, `CERT-PERF-*`, `CERT-PROV-A1-*`, `CERT-RES-*`, `CERT-A11Y-*`, `CERT-VOICE-*`. **None of these is an M1 row.** |
| Scripts | `package.json` | `certify`, `cert`, `cert:room`, `cert:provider`, `cert:baselines`, `cert:check`, `gates:check`, `evidence`, `verify`, `release-check` |
| Workflows | `.github/workflows/` | `playwright.yml` (runs `--project=web-chromium` over the whole testDir, then `scripts/evidence.mjs`), `certification-guard.yml`, `architecture.yml`, `typecheck.yml`, `lint.yml`, `format.yml`, `docs-validate.yml` |

Because `playwright.yml` runs the whole test directory rather than an enumerated list, **no CI workflow change is needed** for any new M1 spec. This keeps the "do not modify CI workflows" constraint satisfiable through the whole of M1 except, possibly, for the mobile project question in §3.

---

## 6. Blocking severity ranking and recommended execution order

Severity is taken from the K-launch-certification "blocking" column, not reassigned here.

| Order | Rows | Severity | Rationale |
| --- | --- | --- | --- |
| 1 | Multi-identity fixture (prerequisite, not a row) | — | Ten of fourteen rows need 2–8 concurrent identities. Nothing else can start first |
| 2 | CERT-ROOM-01, CERT-ROOM-02 | Blocker | Entry path to every other journey |
| 3 | CERT-PRES-01, CERT-WP-02 | Blocker / Major | Reuse the same fixture with no new instrumentation |
| 4 | CERT-EXP-01, CERT-EXP-02 | Blocker / Major | Lowest coupling; extends an existing spec |
| 5 | CERT-ROOM-03, CERT-ROOM-04, CERT-PRES-02 | Major / Blocker | Need capacity provisioning and PROF-04 severance |
| 6 | CERT-WP-01 | Blocker | Needs timestamp instrumentation; may reveal a production-code dependency |
| 7 | CERT-SYNC-C-01, CERT-PROV-01, CERT-PROV-02 | Blocker | Depend on stable room and countdown coverage beneath them |
| 8 | Registry wiring (WP2) | — | Only meaningful once every row can emit a record |
| 9 | CERT-SYNC-C-02 | Blocker | Gated on the §3 governance ruling, not on engineering effort |

---

## 7. Unresolved unknowns

Listed rather than guessed. Each is **Unknown** in the strict sense that the repository cannot establish the answer.

1. **U-01.** Whether adding a mobile-viewport Playwright project is permitted under the M0.5 Constitutional Limit in `docs/blueprint/I-governance.md`. Requires a human ruling or a numbered ADR.
2. **U-02.** Whether per-client countdown-zero timestamps are observable without adding instrumentation to `src/domain/countdown/countdown-runtime.ts`. Determining this requires running a spec, which is out of scope for a discovery spike.
3. **U-03.** Whether `CERT-EXP-01` should be emitted as its own record or defined as a roll-up of the existing `CERT-A11Y-*` prefix set. Both are defensible; the choice changes the registry entry shape.
4. **U-04.** The authoritative list of "all launch surfaces" for `CERT-EXP-01`. The route tree is known, but no document enumerates which routes are launch surfaces.
5. **U-05.** Whether the certification environment can provision nine identities within the harness timeout, needed by `CERT-ROOM-03`.
6. **U-06.** Which of `docs/blueprint/J-technical-debt.md` and `docs/debt/debt-register.json` holds the correct meaning of `DEBT-005`.
7. **U-07.** Whether the roadmap's inclusion of `CERT-VOICE-01/02` in the M1 row set is intentional given the M3 dependency, or a roadmap defect.

---

## 8. Summary

- Fourteen M1 rows investigated. **Zero are "Runnable now."**
- Thirteen are **Harness missing**. One (`CERT-SYNC-C-02`) is **Environment unavailable**.
- All fourteen are **absent from the mandatory evidence registry**, so an M1 run today could seal without containing a single M1 record.
- Two voice rows are **Blocked by dependency** on PROF-08 / DEBT-005 and are correctly recorded as `blocked`, never `fail`.
- Thirteen rows need **test-only** work followed by a **registry-only** edit. One needs a **certification-only** harness-configuration change whose permissibility is unresolved.
- **No row requires a schema, provider, or architecture change.**

M1 implementation was not performed. M1 remains pending explicit human authorization.

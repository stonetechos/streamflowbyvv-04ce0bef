# M1.6 — WP3 Implementation Report

Sprint: M1.6. Mode: Build. Evidence run: `RUN-M1WP3-001` (commit `a389458`, environment `local-dev`).

Authority order applied: Architecture Constitution → Launch Envelope → `M1-Implementation-Plan.md` → `M1-Backlog.md` → `M1-Product-Experience-Specification.md` → completed reports (dependency reference only).

## 1. Resolved work package

Resolution rule applied against the frozen backlog and the completed reports (WP1, WP2, WP4 done):

| Field              | Value (verbatim from `docs/m1/M1-Backlog.md`)                                                                        |
| ------------------ | -------------------------------------------------------------------------------------------------------------------- |
| Number             | **WP3**                                                                                                              |
| Title              | **Invite resolution hardening** — "Invite link resolves to the intended room across sign-in and cold start"          |
| Owning engine      | **Room**                                                                                                             |
| Certification rows | **CERT-ROOM-01** (profiles PROF-01, PROF-05)                                                                         |
| User journey       | "Guest receives an invite link and reaches the room" — cold start, interposed sign-in, re-open by an existing member |
| Dependencies       | WP1 (complete)                                                                                                       |

WP3 is the lowest-numbered unfinished package and is also, per plan §8 step 2, in the same parallel band as the already-completed WP4. No conflict was found between the roadmap and the completed reports.

**The resolved next unfinished work package was WP3 — Invite resolution hardening.**

## 2. Scope implemented

Only the three conditions the backlog names for `CERT-ROOM-01`: cold start, interposed sign-in, re-open by an existing member. No new row, profile, evidence semantic, invite surface, public-room or host-transfer behaviour.

## 3. Files changed

| File                                                 | Layer                 | WP  | Engine | Row          | Journey                                            |
| ---------------------------------------------------- | --------------------- | --- | ------ | ------------ | -------------------------------------------------- |
| `tests/certification/room/m1-room-lifecycle.spec.ts` | Certification harness | WP3 | Room   | CERT-ROOM-01 | Guest receives an invite link and reaches the room |

No product file, schema, migration, registry entry, CI workflow or certification semantic changed. `src/` is untouched by this sprint.

## 4. Product behavior implemented

**None — and none was needed.** Per plan §9, a product edit requires a failing record. Every leg passed on first honest measurement, so the shipped invite path (`src/routes/join.$code.tsx`, `src/features/auth/pending-destination.ts`, `src/routes/_authenticated.tsx` continuation, `src/domain/rooms/room-flow-service.ts`) is left exactly as it was.

Measured behaviour of the shipped product:

- A cold, signed-out visitor opening `/join/<code>` is parked at sign-in with the invite code and the full destination remembered.
- Signing in **in that same cold context**, through the shipped form with typed credentials and nothing injected, carries the visitor the rest of the way into the intended room.
- An existing member re-opening the same link is returned to the room, not to Home and not to an error.

## 5. Tests added or updated

`CERT-ROOM-01` previously measured a cold park and then, separately, a _transplanted_ session opening the link. That never crossed a sign-in, so the row's own headline condition ("including across sign-in") was asserted rather than measured, and the record that survived on disk carried PROF-01 while the backlog's acceptance criterion asks for "a measured status under PROF-05".

The spec now runs one continuous PROF-05 journey (cold open → parked at sign-in → real sign-in → room) and keeps the transplanted-session leg, correctly relabelled as the PROF-01 _existing-member re-open_ case. The PROF-05 journey is written last, so the row's record carries the profile the acceptance criterion names. All legs remain visible in `index.json`.

## 6. Certification rows executed

| Row                | Profile | Status | Classification | Note                                                                                                                       |
| ------------------ | ------- | ------ | -------------- | -------------------------------------------------------------------------------------------------------------------------- |
| CERT-ROOM-01       | PROF-05 | pass   | Passed         | Cold open parked at sign-in with pending code `ROM-000218`; the same context landed in the intended room after signing in. |
| CERT-ROOM-01 (leg) | PROF-01 | pass   | Passed         | Existing member re-opening the link returned to the room. Recorded in `index.json`.                                        |

No other row was executed as WP3 work. The remaining 37 records in the run are the unchanged M0/M1 suite, re-run only to keep the run complete and sealable.

## 7. Evidence generated

- Run `RUN-M1WP3-001`, sealed, `complete: true`, commit `a389458`.
- Counts: **pass 29 · fail 0 · unmeasured 8 · blocked 2 · total 39**. Run state `BLOCKED`, unchanged and caused by the pre-existing voice rows.
- Record: `tests/certification/evidence/RUN-M1WP3-001/records/CERT-ROOM-01.json`.

## 8. Remaining blockers

| Id             | Blocker                                                                                                    | Owner                           |
| -------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------- |
| U-01           | No `web-mobile` Playwright project; `CERT-SYNC-C-02` stays `unmeasured`.                                   | WP8 (after a governance ruling) |
| U-02           | No machine-readable countdown signal; `CERT-WP-01/02` stay `unmeasured`.                                   | WP6                             |
| B-08/B-09/B-10 | `K-launch-certification.md` `TBD (M0)` cells and the overloaded `DEBT-005` identifier need a numbered ADR. | Governance                      |
| —              | PROF-08 unsupported; the two voice rows stay `blocked` and are explicitly not an M1 package.               | Out of M1                       |

None blocked WP3.

## 9. Deferred work

WP5 (presence readiness and disconnect latency), WP6 (countdown and stage), WP7 (Tier C coordination), WP8 (web-mobile), WP9 (tier disclosure and reversible degraded mode), WP10 (accessibility and reduced motion). Magic-link sign-in as an invite interposition was not measured: the row does not require it and no mail-capture mechanism exists in the harness — Unknown, not claimed.

## 10. Validation summary

| Check                             | Result                                                                         |
| --------------------------------- | ------------------------------------------------------------------------------ |
| `typecheck`                       | Pass                                                                           |
| `eslint` (changed file)           | Pass                                                                           |
| `prettier --write` (changed file) | Applied                                                                        |
| `arch:check`                      | Pass                                                                           |
| `cert:check`                      | Pass                                                                           |
| `gates:check`                     | Pass — GATE-01/02/03                                                           |
| `adr:check`                       | Pass — 16 records                                                              |
| `format:check` (repository-wide)  | Still fails only on pre-existing frozen M1 documents, unchanged by this sprint |

Guardrails: only WP3 implemented · no later work-package scope implemented · ADR-014 preserved (no playback control, no Tier A/B claim) · Launch Envelope unchanged (2–8 private participants, web desktop, Tier C) · no architecture redesign · no certification redesign · every claim above matches the repository.

## 11. Completion verdict

**WP3 is complete.** The row is now measured under the profile its acceptance criterion names, across a real sign-in, and it passes without any product change.

**Is the repository ready to proceed to the next frozen work package?**

Yes. WP5 (Presence accuracy) is the next unfinished package in the frozen sequence; it depends only on WP1, whose fixture and evidence writers are in place and now exercised by three completed product packages. Its own risk — whether disconnect latency is observable at all — remains Needs discovery and must be measured honestly, not asserted. The two governance items (B-08/B-09/B-10) stay open before any M1 seal is claimed.

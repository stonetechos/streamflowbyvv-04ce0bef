# M1.4 — WP2 Implementation Report (Registry and checklist wiring)

Sprint: M1.4. Mode: Build. Scope executed: **WP2 only**, exactly as frozen in `docs/m1/M1-Backlog.md` §WP2 ("Add the M1 rows to the mandatory-evidence registry").

Authority order applied: Architecture Constitution → Launch Envelope → `M1-Implementation-Plan.md` → `M1-Backlog.md` → `M1-Product-Experience-Specification.md`.

---

## 0. Conflict disclosure (read first)

One conflict exists between the frozen documents and the sprint brief, and one internal tension between two frozen documents. Both are reported rather than resolved silently.

| # | Conflict | Frozen source | Disposition |
| --- | --- | --- | --- |
| C-01 | Sequencing. `M1-Implementation-Plan.md` §8.5 and `M1-Dependency-Graph.md` (`WP1 -> WP3 -> WP4 -> WP9 -> WP2 -> seal`) place **WP2 last**, after WP3–WP10. The brief runs WP2 immediately after WP1. | Plan §8, Dependency Graph §3 | **Executed anyway, deliberately.** `M1-Backlog.md` §WP2 states the hard dependency as **WP1 only**, and the plan's stated reason for deferring ("making them mandatory earlier would break the green gate") does not hold in repository reality: WP1 made all fourteen rows emit records, `npm run verify` does not re-validate historical runs, and a full single run now satisfies the enlarged registry (see §9). The deviation is recorded here; the roadmap ordering is otherwise untouched. |
| C-02 | `DEBT-005` denotes two different items — PROF-08 voice in `docs/debt/debt-register.json`, an ADR-header item in the frozen `docs/blueprint/J-technical-debt.md`. WP2-T3 asks for reconciliation. | Backlog §WP2 scope, Risk R-05 | **Not executed — Blocked by policy.** `J-technical-debt.md` is frozen blueprint; renumbering either identifier requires a numbered ADR. No ADR authorises it, so nothing was changed. |

---

## 1. Scope implemented

Implemented, from the frozen WP2 scope line:

- **Entries in `docs/registry/required-evidence.json`** for all fourteen M1 rows — done.
- **Missing harness wiring required for repository integration** — one defect corrected so the fourteen rows can all emit records inside a *single* run (WP1 delivered them across three partial runs). Test-only.
- **Registry enforcement proof** — a negative self-test showing the seal is refused and the missing M1 rows are named.

Not implemented, from the frozen WP2 scope line:

- **`TBD (M0)` owner/location cells in `docs/blueprint/K-launch-certification.md`** (WP2-T2, Risk R-06) — Blocked by policy: frozen blueprint, ADR required.
- **`DEBT-005` reconciliation** (WP2-T3, Risk R-05) — Blocked by policy, as above.

Explicitly not touched: room lifecycle, join flow, lobby, waiting room, countdown runtime, provider launcher, presence, sync, voice, chat, UI, schema, migrations, provider classifications, Constitution, Launch Envelope, CI. No `src/` file was modified. No certification row, profile, evidence field or evidence semantic was added or altered.

---

## 2. Files changed

| File | Change | Type |
| --- | --- | --- |
| `docs/registry/required-evidence.json` | 14 M1 entries added (24 → 38); `version` 1.0.0 → 1.1.0; `$comment` extended to state that `milestone` is inert metadata | Registry |
| `tests/certification/room/m1-room-lifecycle.spec.ts` | CERT-ROOM-02 now seats a previously unseated identity in its own lobby | Test-only |
| `docs/m1/M1.4-WP2-Implementation-Report.md` | This report | Documentation |

New evidence directories: `tests/certification/evidence/RUN-M1WP2-001/`, `RUN-M1WP2-002/`.

No frozen document was modified.

---

## 3. Registry changes

Fourteen entries added to `docs/registry/required-evidence.json`, each `match: exact`, each carrying the owning engine and an inert `milestone: "M1"` traceability field that the mandatory-evidence algorithm in `scripts/lib/evidence-io.mjs` ignores:

| Evidence ID | Owning engine | Frozen source |
| --- | --- | --- |
| CERT-ROOM-01 … CERT-ROOM-04 | Room | K-launch-certification.md; `tests/certification/helpers/m1-rows.ts` |
| CERT-PRES-01, CERT-PRES-02 | Presence | as above |
| CERT-WP-01, CERT-WP-02 | Watch Party | as above |
| CERT-SYNC-C-01, CERT-SYNC-C-02 | Sync | as above |
| CERT-PROV-01, CERT-PROV-02 | Provider | as above |
| CERT-EXP-01, CERT-EXP-02 | Experience | as above |

The mandatory-evidence algorithm, the record schema, the result vocabulary and the profile set are unchanged. `scripts/check-gates.mjs` and `scripts/lib/evidence-io.mjs` were reused as-is; neither was edited.

**Known consequence, stated honestly:** the registry has no milestone scoping and WP2's frozen non-scope forbids adding any. Every run — M0 or M1 — must now contain all 38 identifiers to seal. The already-sealed `RUN-M0R-001` is untouched on disk, but it could not be re-sealed under the enlarged registry. Milestone-scoped mandatory evidence would be a change to the algorithm and therefore requires a decision outside WP2.

---

## 4. Certification mappings completed

| Mapping | Status |
| --- | --- |
| Row → mandatory registry entry (14 rows) | Complete |
| Row → owning engine (in the registry entry) | Complete |
| Row → executable definition and profile pairing (`tests/certification/helpers/m1-rows.ts`, WP1) | Already complete |
| Row → evidence-owner/location cells in `K-launch-certification.md` | **Blocked by policy** — frozen blueprint, ADR required |
| `DEBT-005` identifier reconciliation | **Blocked by policy** — frozen blueprint, ADR required |

---

## 5. Evidence produced

Two real runs, both executed against the running application with the full Chromium build.

**`RUN-M1WP2-001`** — first integration run. 39 records; pass 28 · fail 1 · unmeasured 8 · blocked 2; run state FAILED. The single `fail` was `CERT-ROOM-02` (`duplicate key value violates unique constraint "room_members_uq"`), diagnosed as a harness-ordering defect: CERT-ROOM-01 drives the real join flow, which had already seated the same identity in the shared lobby. The run is preserved as honest history.

**`RUN-M1WP2-002`** — after the harness fix. 39 records; **pass 29 · fail 0 · unmeasured 8 · blocked 2**; run state BLOCKED (correct: `blocked` and `unmeasured` never roll up as pass). All 38 mandatory identifiers present; mandatory-evidence validation passed.

Non-passing rows in `RUN-M1WP2-002`, verbatim classification:

| Row | Status | Reason recorded |
| --- | --- | --- |
| CERT-AUTHZ-05 | unmeasured | No `room_state` row; the monotonic version guard was not exercised (pre-existing, M0 row) |
| CERT-EXP-01 | unmeasured | Only the automatable WCAG subset is clean; contrast/focus-order/AA remainder is not machine-decidable |
| CERT-PRES-02 | unmeasured | No server- or peer-side absence transition within 30 s; absence appears client-derived |
| CERT-PROV-01 | unmeasured | Tier/consequence wording not present on the pre-commit surface reachable without a session |
| CERT-PROV-02 | unmeasured | No fault-injection seam for provider-launch fallback |
| CERT-SYNC-C-02 | unmeasured | No `web-mobile` Playwright project exists (U-01) |
| CERT-VOICE-01/02 | blocked | PROF-08 unsupported — no LiveKit test project (DEBT-005) |
| CERT-WP-01 | unmeasured | No countdown-zero timestamp is exposed to the client surface (U-02) |
| CERT-WP-02 | unmeasured | Stage value not readable from either peer surface |

**Registry-enforcement proof (WP2 acceptance criteria):** a copy of `RUN-M1WP2-002` with `CERT-ROOM-03` and `CERT-WP-01` removed was submitted to `scripts/evidence.mjs`. Result: exit 1, `sealed: false`, `refusedAtStage: "mandatory-evidence-validation"`, `missingEvidence: ["CERT-ROOM-03", "CERT-WP-01"]`. The synthetic run was deleted after the check. An M1 seal is now impossible without every M1 record.

---

## 6. Certification rows now runnable

| State | Rows |
| --- | --- |
| **Runnable now, passing with executed evidence** | CERT-ROOM-01, CERT-ROOM-02, CERT-ROOM-03, CERT-ROOM-04, CERT-PRES-01, CERT-SYNC-C-01, CERT-EXP-02 |
| **Runnable now, executing but unmeasured (product signal missing, not harness)** | CERT-PRES-02, CERT-WP-01, CERT-WP-02, CERT-PROV-01, CERT-PROV-02, CERT-EXP-01 |
| **Blocked** | CERT-VOICE-01, CERT-VOICE-02 (PROF-08 unsupported) |
| **Environment unavailable** | CERT-SYNC-C-02 (no `web-mobile` project) |

WP2 made no row newly passing by itself; it made the fourteen rows *mandatory* and made the full set emit in one run. CERT-ROOM-02 moved fail → pass solely because a harness-ordering defect was corrected; no product code changed.

---

## 7. Remaining blockers

| ID | Blocker | Owning WP per frozen plan | State |
| --- | --- | --- | --- |
| B-01 | No countdown-zero timestamp observable from the client surface (U-02) | **WP6** (Watch-party stage and countdown) | Unmeasured |
| B-02 | Stage value not readable by either peer | **WP6** | Unmeasured |
| B-03 | Absence transition not observable within threshold | **WP5** (Presence accuracy) | Unmeasured |
| B-04 | Tier/consequence wording absent on the pre-commit surface | **WP9** (Provider disclosure) | Unmeasured |
| B-05 | No provider-launch fault-injection seam | **WP9** | Unmeasured |
| B-06 | No `web-mobile` Playwright project; governance ruling Q1 outstanding | **WP8** | Environment unavailable |
| B-07 | PROF-08 voice profile unsupported (DEBT-005) | Out of M1 (M3) | Blocked |
| B-08 | `K-launch-certification.md` still carries `TBD (M0)` owner cells for M1 rows | Governance / ADR | Blocked by policy |
| B-09 | `DEBT-005` overloaded across two documents | Governance / ADR | Blocked by policy |
| B-10 | `npm run format:check` fails on seven frozen `docs/m1/*.md` files (pre-existing, authored in M1.0–M1.2) | Governance / ADR | Blocked by policy — reformatting edits frozen documents |
| B-11 | Registry has no milestone scoping, so M0-only runs can no longer seal | Outside WP2 non-scope | Unknown — needs a decision |

**No blocker in this list belongs to WP3 or WP4.** WP3 (invite resolution hardening) and WP4 (join, capacity, leave/rejoin) are the two work packages whose rows already execute and pass: CERT-ROOM-01 through CERT-ROOM-04 are green with executed evidence.

---

## 8. Deferred work

- `K-launch-certification.md` evidence owner/location cells (WP2-T2) — awaiting a numbered ADR.
- `DEBT-005` reconciliation (WP2-T3) — awaiting a numbered ADR.
- Milestone-scoped mandatory evidence (B-11) — outside WP2's frozen non-scope.
- Prettier formatting of the frozen `docs/m1/*.md` files (B-10) — awaiting a governance ruling on whitespace-only edits to frozen documents.

---

## 9. Validation summary

| Gate | Result |
| --- | --- |
| `format:check` (changed files only) | Pass — `required-evidence.json`, `m1-room-lifecycle.spec.ts` |
| `format:check` (repository) | **Fail — pre-existing**, seven frozen `docs/m1/*.md` files and one `.lovable` plan archive. Not introduced by WP2; see B-10 |
| `lint` | Pass — 0 errors, 21 warnings (limit 25) |
| `typecheck` (`tsc --noEmit`) | Pass |
| `arch:check` | Pass |
| `cert:check` | Pass |
| `gates:check` | Pass — GATE-01, GATE-02, GATE-03 |
| `adr:check` | Pass |
| Full harness, single run | 39 records, 0 fail, all 38 mandatory identifiers present |
| Registry refusal self-test | Pass — seal refused, missing M1 rows named |

No Tier A or Tier B claim appears in any new record, in the registry, or in this report. StreamFlow remains at zero Tier A and zero Tier B capabilities.

---

## 10. Readiness recommendation

WP2 is complete to the extent the repository permits: the registry is wired and enforcing, and every M1 row emits a record in one run. Two WP2 sub-tasks (T2, T3) remain Blocked by policy on frozen blueprint documents and require a numbered ADR — neither blocks product implementation, both block a *governance-clean* M1 seal.

**Is the repository now ready to begin the first product implementation sprint (WP4) according to the frozen roadmap?**

**Yes, with two qualifications.**

Ready, because: the four WP4 rows (CERT-ROOM-01 … CERT-ROOM-04) execute end-to-end against real identities and real RLS, emit schema-valid evidence, and are mandatory in the registry; the multi-identity fixture is stable across two consecutive full runs; and any WP4 regression will now be caught as a recorded `fail` rather than an omission.

Qualification one: all four WP4 rows currently **pass**, so — per `M1-Implementation-Plan.md` §9 ("do not begin any product change until a row has run and failed with recorded evidence") — WP4 has no failing record to justify a product edit today. WP4 should therefore open as a verification-and-hardening sprint, not as a rewrite; any code change must first be justified by a new failing record.

Qualification two: B-08, B-09 and B-10 remain open and are governance items requiring an ADR. They do not block WP4, but an M1 seal should not be claimed while they are open.

Stopped after WP2. No later work package was started.

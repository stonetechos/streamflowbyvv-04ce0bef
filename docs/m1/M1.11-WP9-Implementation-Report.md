# M1.11 — WP9 Implementation Report

**Status: IMPLEMENTED. `CERT-PROV-01` passes; `CERT-PROV-02` remains `unmeasured` with a named blocker.**

---

## 1. Work package

| Field                  | Value                                                                                     |
| ---------------------- | ----------------------------------------------------------------------------------------- |
| **Work package**       | WP9 — Provider disclosure and fallback                                                    |
| **Owning engine**      | Provider                                                                                  |
| **Certification rows** | `CERT-PROV-01` (disclosure before commit), `CERT-PROV-02` (announced reversible fallback) |
| **Journey served**     | A user picks a provider and is told exactly what sync they will get, before committing    |

## 2. Finding WP9-F1 — resolved

The pre-commit selection surface derived `supported` from catalog status and
compliance action alone, so an uncertified `play_pause: supported` row promised
automatic control the product cannot deliver (ADR-014, PROV-A1). The authorized
fix extends the WP7 launch rule into selection rather than introducing a new
service.

## 3. Files changed

| File                                                          | Change                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/domain/providers/provider-catalog-service.ts`            | `ProviderCatalogQuery` accepts an optional `platform`; `deriveSelectionClass` takes `isControlCertified`, resolved via `resolveCapabilityTier`. `supported` is announced only for a certified Tier A tuple; every other allowed, available row falls back to `manual_sync`. Absent platform resolves Tier C, exactly as the launch surface does. |
| `src/foundation/localization/bundles/en.ts`                   | Provider hints corrected: manual-sync states the tier, the consequence ("StreamFlow cannot control this player") and the per-participant access expectation; the certified hint no longer claims start/pause automation; the unverified hint states the manual-sync consequence.                                                                 |
| `src/foundation/localization/bundles/hi-IN.ts`                | Same three strings, matching wording.                                                                                                                                                                                                                                                                                                            |
| `tests/certification/provider/m1-provider-disclosure.spec.ts` | `CERT-PROV-01` replaced with a deterministic measurement against the real Provider Engine modules.                                                                                                                                                                                                                                               |

No schema, migration, CI, provider catalog, or certification-semantics change.

## 4. Certification rows executed

- **`CERT-PROV-01` — pass.** With zero Tier A capabilities certified, a catalog
  row claiming `play_pause: supported` is disclosed pre-commit as `manual_sync`
  (badge "Manual sync"), matching the class WP7's launch surface derives for
  the same tuple, and the hint states the tier, the consequence and the access
  expectation. Registry emptiness, selection/launch agreement, and copy honesty
  are all asserted; the derived `available` status is left intact so the catalog
  claim and the user-visible class stay distinguishable.
- **`CERT-PROV-02` — unmeasured (blocked).** No fault-injection seam exists on
  the provider launch path and the profile mapping is `Needs discovery`. Both
  are outside this sprint's authorization. No fallback claim is made.

## 5. Evidence

Run `RUN-M1WP9-001`. `CERT-PROV-01` recorded `pass` on chromium, firefox and
webkit records; the run was not sealed because unrelated launch-surface rows
could not execute in this environment (firefox and webkit browser binaries are
unavailable in the sandbox). Sealing remains gated on a full-environment run —
`npm run evidence` correctly refuses a partial seal.

## 6. Completion verdict

**WP9 is implemented for `CERT-PROV-01`.** `CERT-PROV-02` stays open behind the
named fault-injection blocker. WP10 (accessibility and reduced motion) is next
and untouched.

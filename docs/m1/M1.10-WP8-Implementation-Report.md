# M1.10 — WP8 Implementation Report

Status: Complete (blocked branch of the frozen acceptance criterion)
Sprint: Frozen M1 roadmap, one work package
Run: `RUN-M1WP8-001`
Commit at measurement: `b9b1bad`

## 1. Resolved work package

**The resolved next unfinished work package was WP8.**

| Field                                 | Value                                                                                                                                                                                                                                                                               |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Number / title                        | WP8 — Web-mobile certification surface ("Execution environment for the web-mobile Tier C row")                                                                                                                                                                                      |
| Owning engine                         | Sync                                                                                                                                                                                                                                                                                |
| User journey served                   | Same as WP7 — host picks a provider, the room hands off externally, everyone presses play together — on a mobile viewport                                                                                                                                                           |
| Certification rows                    | `CERT-SYNC-C-02` (profile PROF-01)                                                                                                                                                                                                                                                  |
| Prerequisite work packages            | WP1; plus a governance ruling on the M0.5 Constitutional Limit                                                                                                                                                                                                                      |
| Repository evidence for prerequisites | WP1 harness present (`tests/certification/helpers/m1-rows.ts`, `tests/certification/fixtures/identities.ts`, `tests/certification/helpers/instrumentation.ts`); WP2 registry entries in `docs/registry/required-evidence.json` v1.1.0; sealed runs `RUN-M1WP6-001`, `RUN-M1WP7-001` |
| Remaining dependencies                | The governance ruling (Q1 / U-01) is **still Unknown**. No ADR in `docs/adr/` rules on harness-configuration changes under I.13                                                                                                                                                     |

Because the ruling is absent, WP8's own acceptance criterion applies its second
branch verbatim: _"Absent the ruling, the row is recorded as `blocked` naming the
missing environment."_ No `playwright.config.ts` change was made.

## 2. Regression check summary (WP1–WP6 after WP7)

- Certification evidence still matches repository behaviour. WP7 changed only
  `classify()` in `src/domain/providers/provider-launch-coordinator.ts`.
- No previously passed row is now blocked, and none is now invalid. The only
  earlier records touching the provider path — `CERT-PROV-01` and `CERT-PROV-02`
  in `RUN-M1WP6-001` — were already `unmeasured`, so no pass depended on the old
  `supported` launch class.
- No dependency changed. WP1/WP2 harness and registry are intact.
- No UI or product wording overclaims capability: the launcher now presents
  coordinated manual sync unless a Tier A certification exists, and the registry
  remains empty.
- `npm run typecheck`, `arch:check`, `cert:check`, `gates:check`, `adr:check` and
  lint (0 errors) all pass. `format:check` still reports pre-existing warnings on
  frozen M1 planning documents authored in earlier sprints; those documents were
  not reformatted because they are frozen.

No regression, no overclaim. Implementation proceeded.

## 3. Scope implemented

Gave `CERT-SYNC-C-02` a real, executable measurement and an explicit, named
blocker instead of silence. Determined and recorded the smallest change that
would give the row an environment.

## 4. Product behaviour implemented

None. WP8 is an execution-environment work package; no product surface, copy,
route, or engine behaviour changed. Watch-party behaviour is byte-identical to
the state sealed at WP7.

## 5. Files changed

- `tests/certification/sync/m1-sync-tier-c.spec.ts` — extracted the Tier C
  hand-off measurement into a shared local function and pointed both rows at it;
  `CERT-SYNC-C-02` now runs the identical measurement when the active Playwright
  project is named `web-mobile`, and otherwise records `blocked` naming the
  missing project, the smallest remediating change, and the governance unknown.

## 6. Existing modules reused

`tests/certification/helpers/m1-rows.ts` (evidence writer),
`tests/certification/fixtures/identities.ts` (provisioning, seating, signed-in
context), `tests/certification/fixtures/backend.ts`, and the WP7 instrumentation
attributes on `provider-launch-panel.tsx`. `playwright.config.ts` was read, not
modified.

## 7. New files added

None. Extension of the existing spec was sufficient; a second spec file would
have duplicated the WP7 measurement and risked two divergent definitions of the
same Tier C guarantee.

## 8. Tests added or updated

`CERT-SYNC-C-02` changed from a static placeholder into a conditional real
measurement. `CERT-SYNC-C-01` is unchanged in behaviour — it now calls the
extracted function and still passes with identical output.

## 9. Certification rows executed

| Row            | Platform    | Status      | Note                                                                    |
| -------------- | ----------- | ----------- | ----------------------------------------------------------------------- |
| CERT-SYNC-C-02 | web-mobile  | **Blocked** | No `web-mobile` Playwright project; ran under `web-chromium`            |
| CERT-SYNC-C-01 | web-desktop | Passed      | Re-emitted by the same spec file; unchanged assertion, unchanged result |

## 10. Evidence generated

`tests/certification/evidence/RUN-M1WP8-001/records/CERT-SYNC-C-02.json` and
`.../CERT-SYNC-C-01.json`.

## 11. Remaining blockers

1. **Governance (Q1 / U-01):** does I.13 permit adding a `web-mobile` Playwright
   project? I.13 forbids new infrastructure but allows a change "scoped to
   removing that blocker and nothing else" when a blocker is found during
   certification. Whether a harness project entry counts as such a change is a
   human ruling, not a repository fact. Until it is ruled, `CERT-SYNC-C-02`
   stays blocked.
2. Smallest identified change, if permitted: one entry in
   `playwright.config.ts` — `{ name: "web-mobile", use: { ...devices["Pixel 5"],
...chromiumLaunchOptions } }`. No new tooling, dependency, workflow, or
   dashboard.

## 12. Deferred work

WP9 (provider disclosure and fallback) and WP10 (accessibility and reduced
motion) remain unstarted. Voice rows remain blocked by dependency on PROF-08
(milestone M3).

## 13. Validation summary

`typecheck`, `arch:check`, `cert:check`, `gates:check`, `adr:check` pass; lint
0 errors; prettier clean on every file this sprint touched; both rows emitted
schema-valid records under `RUN-M1WP8-001`.

## 14. Quality self review

- Exactly one work package implemented — yes.
- WP7 introduced a regression or overclaim — no.
- A completed work package revisited unnecessarily — no; WP7's spec was extended
  only because WP8's row shares that measurement.
- Later roadmap scope introduced — no.
- Existing modules reused, unnecessary abstractions avoided — yes.
- Hearo-style watch-party feel preserved and faithful to the Product Experience
  Specification — yes; no product surface changed.
- Certification results fully supported by evidence, and all product claims
  truthful to certified capability — yes.
- Report claims match repository reality — yes.

## 15. Completion verdict

WP8 is complete on the branch the repository can honestly support: the row is
implemented, executable, and recorded `blocked` with a named blocker. It cannot
reach a measured state without a governance ruling.

**Is the repository ready to proceed to the next frozen work package?** Yes —
WP9 may proceed. `CERT-SYNC-C-02` remains blocked pending the governance ruling
and does not gate WP9 or WP10.

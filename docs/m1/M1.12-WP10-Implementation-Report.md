# M1.12 — WP10 Implementation Report

**Status: IMPLEMENTED. `CERT-EXP-02` passes across all 13 launch surfaces; `CERT-EXP-01` remains `unmeasured` behind the named manual-audit blocker.**

The resolved next unfinished work package was WP10.

---

## 1. Resolved work package

| Field                                   | Value                                                                                                                                                    |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Work package**                        | WP10 — Experience: accessibility and reduced motion                                                                                                      |
| **Title (frozen)**                      | WCAG 2.1 AA sweep and reduced-motion conformance                                                                                                         |
| **Owning engine**                       | Experience                                                                                                                                               |
| **User journey served**                 | Every user, on every launch surface                                                                                                                      |
| **Certification rows**                  | `CERT-EXP-01`, `CERT-EXP-02` (profile PROF-09 per `tests/certification/helpers/m1-rows.ts` lines 129–143)                                                |
| **Prerequisite WPs**                    | WP1 only, for the evidence-writer contract (`docs/m1/M1-Backlog.md`, WP10 → Dependencies)                                                                |
| **Repository evidence for prerequisites** | `tests/certification/helpers/m1-rows.ts` (`recordM1Row`, rows registered); `tests/certification/helpers/instrumentation.ts` (`movingElements`, `unnamedControls`, `documentSemantics`); `docs/registry/required-evidence.json` (WP2 mapping) |
| **Remaining dependencies**              | None. The manual PROF-09 screen-reader/contrast audit is a governance activity, not a code dependency.                                                    |
| **Resolution**                          | WP1–WP9 are sealed; WP10 is the last entry in the frozen backlog whose status is not complete (`Partially complete`). Voice is explicitly not an M1 work package. |

## 2. Dependency review

| Check                                       | Result                                                                                                                          |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Regression in completed work                | None. No product module was touched by this sprint.                                                                             |
| Evidence still matches repository behavior  | Yes — WP9's `CERT-PROV-01` re-measured `pass` against the current tree in this environment.                                     |
| Launch Envelope                             | Unchanged: 2–8 private participants, web desktop, Tier C.                                                                       |
| ADR-014                                     | Preserved; no playback-control surface added or implied.                                                                        |
| Product Experience Specification            | Truthful; surface list used here matches its launch-surface inventory.                                                          |
| Capability overclaim on any UI              | None found; provider selection and launch both derive class from the certification registry (WP7 + WP9).                        |
| Host/member authority                       | Untouched; accessibility sweep is read-only.                                                                                    |

No blocking regression was found.

## 3. Scope implemented

Extended the existing sweeps to the full launch-surface inventory and made the
reduced-motion assertion cover authenticated surfaces, including the room.
Nothing else.

## 4. Product behavior implemented

None — WP10 is a certification-coverage work package. No `src/` change was
required, because both sweeps came back clean on the current product.

## 5. Files changed

| File                                                | Change                                                                                                                                             |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `tests/certification/accessibility/a11y-sweep.spec.ts` | Public route list extended from `/`, `/auth` to all five publicly reachable launch surfaces.                                                     |
| `tests/certification/experience/m1-experience.spec.ts` | Surfaces split into public and authenticated sets; `/home` moved to the authenticated set (signed out it only measures the auth gate); seven authenticated surfaces plus `/rooms/:roomId` are now audited for both rows; unreachable surfaces are named in the record and downgrade `CERT-EXP-02` to `unmeasured` rather than passing on partial coverage. |
| `docs/m1/M1.12-WP10-Implementation-Report.md`       | This report.                                                                                                                                       |

## 6. Existing modules reused

`tests/certification/helpers/instrumentation.ts` (`movingElements`,
`unnamedControls`, `documentSemantics`), `tests/certification/helpers/m1-rows.ts`
(`recordM1Row`), `tests/certification/fixtures/identities.ts`
(`provisionParticipants`, `createRoomWithCapacity`, `seatHost`,
`signedInContext`, including its existing `reducedMotion` option).

## 7. New files added

Only this report; the frozen roadmap requires one implementation report per
work package. No new spec file was needed — the backlog anticipated "a new
reduced-motion spec", but `m1-experience.spec.ts` already owns `CERT-EXP-02`,
so extending it avoids a duplicate spec home for the same row.

## 8. Tests added or updated

Both specs above were updated; no test was added or weakened.

## 9. Certification rows executed

- **`CERT-EXP-02` — Passed.** With `prefers-reduced-motion: reduce`, no element
  on any of 13 launch surfaces (`/`, `/auth`, `/auth/sign-in`, `/auth/sign-up`,
  `/auth/forgot-password`, `/home`, `/people`, `/invites`, `/share`,
  `/account`, `/settings`, `/onboarding`, `/rooms/:roomId`) retained a
  perceptible animation or transition.
- **`CERT-EXP-01` — Unmeasured (blocked).** The automatable subset (WCAG 4.1.2,
  3.1.1, 2.4.2) is clean on all 13 surfaces. Blocker: WCAG 2.1 AA additionally
  requires contrast, focus order and screen-reader review under PROF-09, a
  manual audit required by `docs/blueprint/K-launch-certification.md`. The row
  was not promoted on automation alone.

## 10. Evidence generated

Run `RUN-M1WP10-001`:
`records/CERT-EXP-01.json` (`unmeasured`), `records/CERT-EXP-02.json` (`pass`),
plus five `CERT-A11Y-*` records, all `pass` on chromium. The run is not sealed:
firefox and webkit browser binaries are unavailable in this environment, so
`npm run evidence` correctly refuses a partial seal.

## 11. Remaining blockers

1. `CERT-EXP-01` — manual PROF-09 audit (contrast, focus order, screen reader).
2. `CERT-PROV-02` — no fault-injection seam on the provider launch path.
3. `CERT-SYNC-C-02` — no `web-mobile` Playwright project; I.13 governance unknown.
4. Sealing an M1 run requires an environment with all three browser engines.

## 12. Deferred work

Voice (`CERT-VOICE-01/02`) is not an M1 work package: blocked on PROF-08,
scheduled at M3 under `DEBT-005`. M2 is not begun.

## 13. Validation summary

`tsgo --noEmit` clean. Certification run executed on chromium: 7 passed, 0
failed, with the records listed in §10. Every claim above is traceable to those
files.

## 14. Completion verdict

**WP10 is implemented.** It is the final planned M1 work package.

- All ten M1 work packages (WP1–WP10) are complete.
- Rows not Passed: `CERT-EXP-01` (Unmeasured — manual audit),
  `CERT-PROV-02` (Unmeasured — no fault-injection seam),
  `CERT-SYNC-C-02` (Blocked — no web-mobile environment).
  All other eleven M1 rows have recorded passes.
- M1 is **not** ready for final certification and release review until those
  three rows are resolved and a full three-engine run is sealed.
